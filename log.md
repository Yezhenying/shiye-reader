# 拾页开发日志

> 本文件持续记录 PRD 实现进度、验证结果、重要决策和剩余风险。每轮代码修改后同步更新，最终与代码一并提交 Git。

## 项目信息

- 项目：拾页 · 本地优先的桌面阅读与笔记工具
- 产品基线：`docs/PRD.md`
- 技术基线：`docs/TECHNICAL_SPEC.md`
- 质量基线：`docs/QUALITY_SPEC.md`
- 开始记录：2026-08-17 15:29（UTC+8）
- 当前阶段：Pre-Alpha
- Git 状态：当前目录尚未初始化 Git 仓库；待阶段性实现和验收后统一初始化并提交

## 记录规则

每轮开发均记录：

1. 本轮目标及对应 PRD ID；
2. 修改的文件；
3. 已实现行为；
4. 执行的测试或检查命令及结果；
5. 未完成项、已知风险和下一步；
6. 影响产品/技术基线的决策。

功能只有通过对应质量门禁后才标记为“完成”；仅有界面或原型行为时标记为“部分实现”。

---

## 2026-08-17｜现有代码基线盘点

### 本轮目标

- 建立持续开发日志；
- 盘点当前代码与 PRD 的实际差距；
- 为后续 Git 提交保留可追溯记录。

### 当前已实现

#### 数据与书架（部分对应 P0-01、P0-02、P0-10）

- IndexedDB v1 基础 schema；
- 保存书籍、原文件 Blob、章节、进度、笔记、划线、书签、标签、阅读会话、设置和回收站数据；
- 旧 localStorage 书籍和笔记的基础迁移；
- 导入事务、SHA-256 文件指纹和重复文件检测；
- 书籍状态编辑、筛选、排序、软删除及恢复；
- 存储配额估算和持久存储申请入口。

#### 解析与阅读（部分对应 P0-03、P0-04、P0-05、P0-09、P0-11、P0-13、P0-14）

- EPUB 基础元数据、章节纯文本、目录和封面提取；
- PDF 文本提取、canvas 原页渲染及基础文本层；
- TXT、Markdown 基础文本分段；
- MOBI/AZW3 仅保存原文件并明确提示暂不可阅读；
- Hash 路由、章节切换、书内文本搜索、阅读进度、书签；
- 日间、夜间、暖色主题以及字号、行距、正文宽度设置。

#### 笔记、划线与搜索（部分对应 P0-07、P0-08、P0-16）

- 新建和编辑笔记；
- 标签创建；
- 文本选择后创建四色划线及转为笔记；
- 书名、作者、ISBN、笔记和标签全局搜索。

#### 首页、统计、备份与离线（部分对应 P0-06、P0-12、P0-15）

- 首页与近 7 日统计读取本地真实数据；
- 完整 ZIP 备份，包含业务快照、封面和原始文件；
- 恢复前执行结构与 SHA-256 校验；
- PWA manifest、Service Worker 和生产资源预缓存。

### 当前验证

执行：

```bash
npm run check
```

结果：

- `textPolish`：10 assertions passed；
- `bookUtils`：10 assertions passed；
- `domain`：10 assertions passed；
- `db schema`：9 assertions passed；
- `backup`：2 assertions passed；
- `ebookParser`：14 assertions passed；
- Vite 生产构建成功。

### 尚未达到完成标准

#### M1 数据底座

- IndexedDB schema 尚未完全符合技术规范中的 source、chunk、generation、trash entry 模型；
- 导入没有 Worker、分块写入、完整 staging 可见性和取消清理；
- 文件魔数、ZIP bomb、DRM、异常事务和配额故障矩阵不完整；
- 备份恢复仍是全库替换，没有冲突策略、ID 重映射及恢复 staging；
- 分类、彻底删除、文件替换和 locator 重定位不完整；
- 离线“资源已就绪”尚未验证完整资源 checksum 和四浏览器冷启动流程。

#### M2 阅读器

- EPUB 当前以纯文本阅读为主，缺少安全 XHTML/CSS/图片/字体渲染、内部链接和 CFI；
- PDF 缺少完整 annotation/accessibility layer、页面虚拟化和精确选择定位；
- TXT 缺少 GB18030 等编码检测和手动切换；
- Markdown 尚未进行安全的结构化渲染；
- Locator 和进度恢复只能近似到章节，不能满足语义段落/页内误差门禁；
- 路由、返回和阅读设置尚无真实浏览器 E2E 证据。

#### M3 笔记闭环

- 划线尚未保存真实 DOM/PDF range；
- 笔记缺少 500ms 自动保存、失败草稿、revision CAS 和多标签页编辑租约；
- 标签重命名、合并、删除及引用一致性未完成；
- 笔记、划线和书签的回收站生命周期不完整。

#### M4 发布质量

- 尚未迁移 TypeScript；
- `src/App.jsx` 仍集中较多页面和业务逻辑；
- 缺少组件、集成、Playwright E2E、axe、视觉回归、性能和真实文件 fixture；
- 尚未执行 Chrome、Edge、Firefox、Safari 发布矩阵。

### 阶段判断

当前是“Phase 1 数据底座原型 + 部分 Phase 2/3 功能原型”。依据质量规范，M1、M2、M3 均不能标记为完成。

### 下一步

优先收口 M1：

1. 建立测试框架和 PRD 需求追踪基础；
2. 完善导入校验、任务 staging 和失败清理；
3. 完善书籍 CRUD、回收站与数据不变量；
4. 加固完整备份/恢复事务；
5. 添加真实浏览器核心路径测试；
6. 通过 M1 阶段门禁后再集中实现阅读器规范。

### 本轮修改文件

- 新增：`log.md`

### 风险与说明

- 当前工作目录不是 Git 仓库，暂时无法生成提交历史；
- 最终提交前将检查生成物、临时浏览器目录和日志文件，配置 `.gitignore`，运行完整检查后再由助手执行 Git 初始化、暂存和提交。

---

## 2026-08-17｜Alpha 可信纵切加固

### 本轮目标

- 在不宣称完整 P0/v1 的前提下，收口“导入 → 阅读 → 定位划线/笔记 → 刷新恢复 → 回收站恢复 → 快照导出”核心路径；
- 优先修复迁移截断、字段丢失、笔记改写、导入伪装/ZIP 资源耗尽、恢复定位、备份输入和外部网络请求风险。

### 已实现

- IndexedDB 升级至 v2，新增 `drafts` store；500ms 草稿保存、关闭前强制刷新、刷新后恢复及保存状态可见；笔记正文按输入原样持久化，不再自动调用语义润色；
- `normalizeBook` 保留指纹、活动文件、目录、语言、能力和解析警告等持久字段；旧版迁移改为无数量截断、解析失败不标成功，并以确定性 ID 执行幂等救援；
- 导入编排抽至 `services/importService.js`，刷新后仍可按 SHA-256 检测重复；PDF/EPUB 校验签名，EPUB 增加路径、条目数、展开大小和压缩比限制，固定版式降级为仅保存文件；TXT/Markdown 使用严格 UTF/BOM 检测并在运行时支持时回退 GB18030；
- 文本 locator 保存 offset/quote/prefix/suffix/章节进度，PDF 保存页码/页内进度；恢复、书签、搜索和来源定位尝试回到实际文字偏移，无法匹配时明确降级；划线重新渲染、改色、删除/恢复及转笔记可用；PDF 文档按 Blob 在阅读会话内复用；
- 活跃时间按最大 60 秒活动间隔累计，长时间空闲后一次操作不再回算整个空闲段；右侧记录面板改为显式开关；
- 书架增加分类创建、筛选、分配和事务性删除/解绑；书籍、笔记和划线回收站均有恢复入口；
- 快照恢复增加容器、路径、资源、manifest、记录主键、数量及引用校验，并在替换前展示内容影响；产品文案改为“当前版本完整快照”；
- 移除 Google Fonts 请求和远程封面直接渲染；新增错误边界、模态框焦点圈定/返回、全局可见焦点、最小点击目标、减弱动画和自适应书架网格；Service Worker 通过版本化消息验证缓存完整性后才宣称离线资源已验证。

### 验证

- `npm run test`：通过；覆盖字段保留、PDF 页进度、101 本旧书无截断、80 条旧笔记无截断、伪装 PDF/EPUB 拒绝和 drafts schema；
- `npm run build`：Vite 生产构建成功；
- Chrome Headless 生产烟测：通过真实 TXT 导入、阅读、精确划线、刷新重显、划线转笔记、笔记删除/恢复，以及当前版本快照导出后在全新浏览器配置中恢复；页面标题为“拾页 · 本地阅读与深度笔记”。

### 剩余风险

- EPUB 仍是实验性纯文本提取，不保留规范排版/图片/内部链接，也不支持固定版式阅读；
- PDF 仍是基础页面和文本层，没有可靠矩形划线、OCR、结构/表单/附件支持；
- 导入、哈希、PDF 解析及 ZIP 快照仍为主线程整文件操作，不承诺 500MiB 或流式安全；
- 尚未完成多标签编辑租约、跨标签阅读会话去重、标签重命名/合并、30 日自动清理、替换文件 locator 迁移或跨版本快照合并；
- 本轮 Chrome 烟测不等同于四浏览器离线冷启动、辅助技术矩阵或 WCAG 2.2 AA 验收。

---

## 2026-08-17｜独立复核修复轮 1

### 修复内容

- 旧 `localStorage` 救援升级为 `books/sections/notes/meta` 单事务提交，完成标记与数据同成败；新 v3 救援会识别旧版已写入书籍并补齐确定性章节。
- 完整备份升级为 schema v2，仍接收并补齐无 `drafts` 的 schema v1；导出前校验当前快照，导出后用同一检查器复检 ZIP。增加记录数量/大小/深度、主键、书籍—文件—进度—分类—标签—划线—笔记—书签—设置关系，以及二进制固定路径、归属、大小和 checksum 校验。
- 进度 revision 改由 IndexedDB 单个读写事务读取当前值并单调递增，写回值直接同步当前标签页状态；数据库 `blocked` 打开失败会清除缓存 promise，允许用户关闭阻塞标签页后重试。
- 阅读活动按 `min(gap, 60s)` 计入并每 5 秒落盘/同步当前页；统计按本地自然日拆分跨日会话，连续天数不再限于七日，并纳入当日创建的笔记/划线。
- 修正选择两端空白导致的 quote/offset 偏移，重定位优先结合 prefix/suffix；大小写不敏感书内搜索使用原文 quote 导航，失败不再宣称精确命中。
- PDF 文本层支持选择后创建页级 locator 的划线/笔记；换页时取消旧 canvas render task，外部 HTTP(S) 链接需确认后才以 `noopener,noreferrer` 打开。
- 笔记草稿 dirty 判定覆盖类型、正文和标签；关闭前总是强制保存未提交草稿。导入/恢复文件 input 每次重置，导入解析/提交期间禁用关闭并明确“不支持取消”。
- 全局笔记结果定位并聚焦实际卡片，标签结果打开真实过滤视图；窄屏目录和记录面板均为可用的左右抽屉，不再隐藏记录或保留不可见栏。
- 为图标按钮、modal 关闭、设置 range、主题和颜色状态补充 accessible name/pressed state；离线状态改为“不可用/准备中/已缓存”，不再把缓存存在描述成密码学意义上的完整性校验。
- 增加适配 Vite、Blob 图片、PDF.js worker 与 Service Worker 的限制性 CSP；重复指纹命中回收站书籍时提供“恢复并打开”或“保留副本”。

### 验证

- `npm run test`：退出码 0；`domain` 21 assertions、`db schema` 13 assertions、`backup` 11 assertions，其余解析/工具测试全部通过。
- `npm run build`：退出码 0；Vite 生产构建成功。
- `git diff --check`：退出码 0（仅 Git 的 LF→CRLF 工作区提示，无 whitespace error）。
- 当前生产构建 Chrome 151 CDP 烟测：退出码 0。可复现步骤：

```bash
npm run preview -- --host 127.0.0.1 --port 4199 --strictPort
"C:/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu --remote-debugging-port=9225 --user-data-dir="G:/Pi/.chrome-fix-final" http://127.0.0.1:4199/
node tests/chrome-smoke.mjs
```

- 烟测从新配置到达真实 dashboard，执行 TXT 导入 → reader → 选择“本地优先”创建杏黄划线 → 创建笔记 → 滚动保存进度 → reload。刷新前 IndexedDB 计数为 books/highlights/notes/progress 各 1；刷新后仍在 reader，划线文本仍为“本地优先”。结构化证据保存在 `tests/evidence/fix-round-1.json`。

### 剩余风险

- PDF 标注当前是可恢复的页码 + 文本上下文 locator，并非矩形几何覆盖；复杂 PDF 文本顺序、扫描件/OCR 仍可能无法选择或精确重定位。
- 进度 revision 已原子单调，但仍采用数据库实际提交顺序的最后写入值；尚无跨标签编辑租约或用户可见冲突合并。
- 阅读会话会实时更新并正确限制空闲补计，但跨标签页同时阅读的重叠区间尚未做 leader lease/并集合并。
- 备份仍在主线程内存中组装 ZIP 并复检二进制；大书库没有流式导出。schema v1 只承诺迁移本实现旧快照（主要差异为缺少 drafts），不宣称任意未来/外部格式兼容。
- 本轮新增的是 Chrome 核心纵切证据；尚未完成 PDF fixture、备份恢复浏览器纵切、四浏览器、axe 或完整键盘/屏幕阅读器矩阵。

---

## 2026-08-17｜交付前独立复核修复轮 2

### 修复内容

- 选择记录现在绑定原章节；切换目录、搜索结果或上一/下一章时会清除选区，保存前再次验证章节，避免把旧选区写入新章节。
- TXT/Markdown 规范化后若只含空白字符会阻止导入并给出可恢复错误；阅读器对任何零章节异常数据均提供安全空状态。
- 全局快捷键在输入框、可编辑控件、对话框或已有全局 modal 时停用，避免笔记编辑中叠加第二个对话框。
- 移动端顶栏使用真实 label span 并在窄屏隐藏文字；书架工具栏可换行，在 420px 以下切为单列；375px 页面无横向溢出。
- 备份恢复新增字段级运行时校验：书籍标题/作者/格式/能力/状态、revision、时间戳，文件大小，章节正文，进度/Locator，笔记类型，划线颜色，书签、阅读会话等均在替换数据库前验证；恶意对象类型书名会被拒绝。
- 恢复流程增加跨标签页写入屏障通知。其他标签页收到 `restore-begin` 后暂停进度、会话、草稿和业务写入，收到 `restore-complete` 后重新加载；发起页在屏障内完成原子替换。
- `.gitignore` 扩充 Chrome/CDP 与临时进程产物，移除误纳入基线的 `chrome-smoke.err`。

### 最终验证

- `npm run check`：退出码 0；`backup` 增至 12 assertions，全部 Node 测试通过，Vite 生产构建成功。
- `git diff --check`：退出码 0，无 whitespace error。
- 当前生产构建 Chrome 核心纵切再次通过，证据：`tests/evidence/final-smoke.json`。
- 375×812 Chrome 边界烟测通过：页面宽度与视口同为 375px、移动顶栏标签正确隐藏、书架工具栏为 grid、modal 内 Ctrl+N 未叠加对话框、纯空白 TXT 被拒绝；证据：`tests/evidence/final-edge-smoke.json`。
- 可复现脚本：`tests/chrome-smoke.mjs`、`tests/ui-edge-smoke.mjs`。

### Git 记录

- 已初始化 Git，并创建原型基线提交 `44ff707`；
- 本轮最终代码将在全部检查通过后创建独立交付提交。

### 诚实能力边界

- 当前交付是可实际使用的本地优先 Alpha 网页，并非质量规范定义的 Stable v1；
- EPUB 为实验性纯文本阅读，Markdown 为纯文本阅读，PDF 为基础页面/文本层和页级文本上下文标注；界面已明确披露；
- 尚未完成 Worker/流式大文件、EPUB XHTML/CSS/资源阅读系统、PDF 几何标注、跨标签阅读时长 leader lease、完整标签管理、文件替换重定位、四浏览器/axe/WCAG 证据矩阵。
